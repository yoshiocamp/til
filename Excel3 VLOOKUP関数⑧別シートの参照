参照したい表は同じファイルの別シートに構えておくことが大事
列や行を削除すると思わぬデータまで消してしまう事がある為

やり方は簡単で
VLOOKUPの範囲選択の時に別シートをクリックして使いたい表を範囲参照するだけ
すると
=VLOOKUP(検索値,  シート名!範囲,列番号,検索方法)になる

このシート名！が他のシートを参照しているというしるしになっている


しかしエクセルの中には同じシート内にないと使えないものもある
例
WHAT IFのテーブルなど
