python3.8以降に登場
例
もし3文字以上の文字列だったらその長さを出力する
s = "Python"
if len(s) >= 3:
   print(len(s))

6                    #これでも問題はないが見にくい

セイウチ演算子

s = "Python"
if （n := len(s)） >= 3:  #変更内容を()で囲いlenの前に　n :=  を入れる。文字列の中身が3以上であるか判定しながら変数nに文字列の長さを代入できる
   print(n)　　　　　　#代入したnを出力する

使う機会は多くない
